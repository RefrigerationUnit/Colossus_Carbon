// Reserved for future: draw airports, seaports, or reference layers on the globe.
// For the MVP we only toggle factors/summary, so this file intentionally minimal.

export function applyOverlays(globe, activeLayers) {
  // globe.setCustomLayer('airports', activeLayers.flight ? data : []);
  // globe.setCustomLayer('seaports', activeLayers.ship ? data : []);
}
